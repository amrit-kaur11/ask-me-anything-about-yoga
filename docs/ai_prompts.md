# AI Prompt Log (AskMe AI — Yoga RAG)

This file records prompts used during development for evaluation transparency.

## Prompt — Architecture
"Provide a paste-ready FastAPI + MongoDB + FAISS RAG micro-app for yoga with /api/ask and /api/feedback endpoints, a safety heuristic layer, and a React UI that shows answer + sources + warning block."

## Prompt — Ollama Integration
"Replace OpenAI with Ollama for LLM generation (primary llama3.1:8b, fallback mistral:7b) and ensure the app never fails hard if the local LLM is unreachable."
